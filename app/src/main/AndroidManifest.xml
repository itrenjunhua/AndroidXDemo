<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.renj.jetpack">

    <application
        android:name="com.renj.jetpack.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackDemo">
        <activity
            android:name="com.renj.jetpack.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".fvp.FVPActivity"
            android:label="Fragment、Fragment懒加载、ViewPager、ViewPager2的实现" />
        <activity
            android:name=".navigation.NavigationActivity"
            android:label="Navigation 组件使用" />
        <activity
            android:name=".paging.PagingListActivity"
            android:label="Paging 框架使用" />
    </application>

</manifest>